#include<stdio.h>
int main()
{
	int a,b,result=0,choice;
	printf("enter the two numbers of ur choice :");
	scanf("%d%d",&a,&b);
	switch(choice){

case1:	printf("the addition of the numbers given are:");
	result=a+b;
	scanf("%d",&result)
	break;
case2:	printf("the subtraction of the two numbers are:");
	result=a-b;
	scanf("%d",&result)
	break;
case3:	printf("the multiplications of the two provided no are:");
	result=a*b;
	scanf("%d",&result);
	break;
case 4:	Printf("the division of the two given numbers are:");
	result=a/b;
	scanf("%d",&results);
	break;
case 5: default
	exit 0;
}
return 0;
}
